// Kept as a safe fallback only.
// Real production service worker is generated by vite-plugin-pwa.
self.addEventListener('install', () => {
  self.skipWaiting();
});

self.addEventListener('activate', (event) => {
  event.waitUntil(self.clients.claim());
});
